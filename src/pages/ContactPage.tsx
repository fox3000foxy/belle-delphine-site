import { Box } from '@mui/material';
import Contact from '../components/Contact';

const ContactPage = () => {
  return (
    <Box sx={{ pt: 10 }}>
      <Contact />
    </Box>
  );
};

export default ContactPage;
